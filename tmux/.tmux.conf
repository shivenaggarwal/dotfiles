##### Prefix Configuration #####
unbind C-b
set -g prefix C-a
bind C-a send-prefix

##### General Settings #####
set -g base-index 1              # Start window indexing at 1
set-option -g default-terminal "screen-256color"
set -g mouse on                  # Enable mouse support
setw -g mode-keys vi             # Use vi keys in copy mode-keys
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy" #enable copy mode


##### Pane Navigation with hjkl #####
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

##### Pane Resize #####
bind -n M-h resize-pane -L 5
bind -n M-l resize-pane -R 5
bind -n M-k resize-pane -U 5
bind -n M-j resize-pane -D 5

##### Plugin Manager (TPM) #####
set -g @plugin 'christoomey/vim-tmux-navigator'
# Add the Oxocarbon theme plugin
# set -g @plugin 'ntk148v/oxocarbon-tmux'
# set -g @plugin 'mkoga/tmux-solarized'
#set -g @plugin 'z3z1ma/tmux-gruvbox'
## ...alongside
#set -g @plugin 'tmux-plugins/tpm'
#set -g @gruvbox_flavour 'material'
#set -g @plugin 'egel/tmux-gruvbox'
#set -g @tmux-gruvbox 'dark'
#set -g @plugin "adibhanna/gruvbox-tmux"
#set -g @gruvbox-theme dark # dark or light
#
## Optional: Configure GitHub status display (default: on)
#set -g @gruvbox-tmux_github_status on # on or off

#
# The run command for the theme has been removed from here
# and will be handled by TPM along with other plugins at the end of the file.
#

##### Status Bar Styling #####
# set -g status-position bottom
# set -g status-bg colour234
# set -g status-fg colour137
# set -g status-left ''
# set -g status-right '#[fg=colour233,bg=colour241,bold] %d/%m #[fg=colour233,bg=colour245,bold] %H:%M:%S '
# set -g status-right-length 50
# set -g status-left-length 20
# setw -g mode-keys vi

# setw -g window-status-current-format ' #I#[fg=colour250]:#[fg=colour255]#W#[fg=colour50]#F '
# setw -g window-status-format ' #I#[fg=colour237]:#[fg=colour250]#W#[fg=colour244]#F '


# ##### Pane Borders (Using generic colors that fit the theme) #####
# set -g pane-border-style fg=colour238
# set -g pane-active-border-style fg=colour137

# ##### Message Styling (Using generic colors that fit the theme) #####
# set -g message-style bg=colour234,fg=colour137
# set -g message-command-style bg=colour234,fg=colour245

##### Initialize TMUX Plugin Manager (Keep at bottom) #####
run '~/.tmux/plugins/tpm/tpm'

##### Override ONLY status bar content (no color overrides) #####
# set -g status-left ''
# set -g status-right '%d/%m %H:%M:%S'
# set -g status-right-length 50
# set -g status-left-length 20
# set -g status-justify left

# setw -g window-status-current-format ' #I:#W '
# setw -g window-status-format ' #I:#W '

##### Status bar override — Solarized Light pastel match #####

# Clean left side
# set -g status-left ''

# # Right side date/time
# set -g status-right '#[bg=colour254,fg=colour246] %a %d %b #[bg=colour254,fg=colour160] %H:%M '
# set -g status-right-length 50
# set -g status-left-length 20
# set -g status-justify left

# # Window list
# # Inactive windows: light gray text on pale background
# setw -g window-status-format '#[bg=colour254,fg=colour246] #I  #W '

# # Active window: muted violet highlight with light text
# setw -g window-status-current-format '#[bg=colour62,fg=colour230] #I  #W '

##### Status bar override solarized light latest#####

# set -g status-left ''

# # Right side date/time
# set -g status-right '#[bg=colour254,fg=colour241] %a %d %b #[bg=colour254,fg=colour160] %H:%M '
# set -g status-right-length 50
# set -g status-left-length 20
# set -g status-justify left

# # Inactive windows:
# # - bg: pale cream
# # - number: blue
# # - name: gray
# setw -g window-status-format '#[bg=colour254,fg=colour33] #I #[fg=colour241]#W '

# # Active window:
# # - bg: pale cream
# # - number: red
# # - name: gray
# setw -g window-status-current-format '#[bg=colour254,fg=colour160,bold] #I #[fg=colour241]#W '

##### Gruvbox-Material Status Bar #####

# Set base bar background and foreground
# set -g status-bg colour235
# set -g status-fg colour248

## everything below this for Gruvbox-Material status bar
set -g status-style bg=colour235,fg=colour248

# Right side content (date + time)
set -g status-right '#[bg=colour235,fg=colour248] %a %d %b #[bg=colour235,fg=colour167] %H:%M '
set -g status-right-length 50
set -g status-left-length 20
set -g status-justify left

# Clean left side
set -g status-left ''

# Window titles
setw -g window-status-format '#[bg=colour235,fg=colour109] #I #[fg=colour248]#W '
setw -g window-status-current-format '#[bg=colour235,fg=colour167,bold] #I #[fg=colour248]#W '

# Pane borders
set -g pane-border-style fg=colour240
set -g pane-active-border-style fg=colour109

# Messages
set -g message-style bg=colour235,fg=colour248
set -g message-command-style bg=colour235,fg=colour214


